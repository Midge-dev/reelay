# Reelay — Cross-Platform Design Tokens

Superseded by the **Nocturne** design system. Canonical values now live in
[`docs/tokens.json`](tokens.json); the rules a token table can't carry (focus
signal, motion budget, scrim policy, porting notes) live in
[`DESIGN.md`](../DESIGN.md) at the repo root. This file's old values (the
retired near-black/neon-purple Kotlin/Compose palette) no longer apply to any
shipped platform — see `NOTES.md` for that history.
